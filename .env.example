# Copy to .env and adjust for your environment
SECRET_KEY=your-secret-key-here
DEBUG=True

# PostgreSQL / Neon (optional — SQLite used by default)
# DATABASE_URL=postgresql://user:password@host/dbname
